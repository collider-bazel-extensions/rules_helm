apiVersion: v2
name: sub
description: Trivial subchart used to exercise the multi-Chart.yaml path-resolution logic in helm_template / helm_package / helm_lint.
type: application
version: 0.1.0
appVersion: "1.0.0"
